using UnityEngine;

public class MySillyThing : MonoBehaviour
{
    int meepis = 1;
    float myFloat = 3.123123f;
    string myString = "epic gamer game";
    Vector3 myVector = new Vector3(1, 2, 3);
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        //Debug.Log("MySillyThing Updated!");
    }
}
